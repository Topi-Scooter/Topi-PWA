import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ProfilePage(props: Props): ReactElement {
    return (
        <div>
            Profile Page
        </div>
    )
}
